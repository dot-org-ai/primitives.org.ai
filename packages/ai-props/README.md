# ai-props

AI-powered props primitives for intelligent component properties.

## API

- `AI()` - Wrapper around a React Component with a schema definition or named type. Props are automatically generated if not passed in, using any provided values as context for object generation.
