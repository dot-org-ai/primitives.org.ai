# Wrangler configuration for RPC transport tests
# Used by @cloudflare/vitest-pool-workers

name = "rpc-transport-tests"
main = "./test-worker.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "test"
