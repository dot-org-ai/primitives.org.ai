# ai-workflows

AI-powered workflow primitives for orchestrating intelligent processes.

## API

- `Workflow()` - Define workflows
- `StateMachine()` - Define state machines
- `on()` - Event handlers
- `every()` - Scheduled recurring tasks
- `send()` - Send messages/events
- `schedule()` - Schedule future tasks
